#To find remainder of division operation where the dividend and divisor are both natural number.

dividend = int(input("Enter dividend :- "))
divisor = int(input("Enter divisor :- "))

remainder = dividend % divisor

print ("reamainder : " + str(remainder))
